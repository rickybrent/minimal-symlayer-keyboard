<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?android:attr/colorBackground">

    <LinearLayout
        android:id="@+id/picker_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="8dp">

        <ImageButton
            android:id="@+id/emoji_close_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:contentDescription="Close Emoji View"
            android:padding="8dp"
            android:background="@drawable/ic_emoji_btn_down"
            android:visibility="gone" />

        <ImageButton
            android:id="@+id/emoji_view_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:contentDescription="Emoji Search"
            android:padding="8dp"
            android:background="@drawable/ic_emoji_btn_left" />

        <EditText
            android:id="@+id/search_bar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Search emojis..." />
        <TextView
            android:id="@+id/picker_title"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:text="Sym Layer View"
            android:visibility="gone"
            />
        <ImageButton
            android:id="@+id/clipboard_button"
             android:layout_width="28dp"
            android:layout_height="28dp"
            android:contentDescription="Clipboard History Search"
            android:background="@drawable/ic_clipboard_search_btn" />
        <ImageButton
            android:id="@+id/sym_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:contentDescription="Symbols View"
            android:padding="8dp"
            android:scaleType="fitCenter"
            android:background="@drawable/ic_sym_btn_right" />
        <ImageButton
            android:id="@+id/sym_close_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:contentDescription="Close Symbols View"
            android:padding="8dp"
            android:scaleType="fitCenter"
            android:background="@drawable/ic_sym_btn_down"
            android:visibility="gone" />
    </LinearLayout>

    <FrameLayout
        android:id="@+id/picker_content_area"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <TextView
        android:id="@+id/empty_clipboard_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center_vertical"
        android:text="Clipboard is empty"
        android:paddingBottom="85dp"
        />
</LinearLayout>